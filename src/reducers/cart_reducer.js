import {
  REMOVE_CART_ITEM,
  TOGGLE_CART_ITEM_AMOUNT,
  CLEAR_CART,
  COUNT_CART_TOTALS,
} from '../actions';

const cart_reducer = (state, action) => {
  return state;
};

export default cart_reducer;
